allowBuilds:
  # sharp is used by scripts/generate-brand-assets.mjs, by Payload's image
  # resizing, and by next/image in production, so its native binary has to be
  # built.
  sharp: true
  unrs-resolver: true
  # Payload's admin bundle and its lexical editor go through esbuild.
  esbuild: true
